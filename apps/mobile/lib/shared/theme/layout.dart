/// CreatorHub layout constants.
abstract final class Layout {
  /// Horizontal screen padding
  static const double screenPaddingH = 16;

  /// Card border radius
  static const double cardRadius = 12;

  /// Button border radius
  static const double buttonRadius = 12;

  /// Input border radius
  static const double inputRadius = 12;

  /// Bottom sheet border radius (top corners)
  static const double sheetRadius = 20;

  /// Chip border radius
  static const double chipRadius = 20;

  /// Avatar border radius (fully circular — use half of size)
  static const double avatarRadius = 999;

  /// Bottom navigation bar height
  static const double bottomNavHeight = 64;

  /// Bottom sheet handle width
  static const double sheetHandleWidth = 40;

  /// Bottom sheet handle height
  static const double sheetHandleHeight = 4;

  /// Minimum tap target size (accessibility)
  static const double minTapTarget = 44;

  /// Card content padding
  static const double cardPadding = 16;

  /// Card compact padding
  static const double cardPaddingCompact = 12;

  /// Maximum content width (for tablet/web centering)
  static const double maxContentWidth = 600;

  /// Bottom sheet max height factor
  static const double sheetMaxHeightFactor = 0.9;
}
