# E5.1 — Tasks

> 14 tasks. Decisions locked: single-column layout, SVG mood icons, stylised map placeholder, featured-itinerary chapter source.

| ID | Task | Files touched (new in **bold**) |
|----|------|--------------------------------|
| T1 | API helper `getFeaturedChapterStory()` — fetches first `featured=true` itinerary + its spots from existing API. Returns null if no featured itinerary (graceful degrade). | [apps/web/src/lib/api/feed.ts](apps/web/src/lib/api/feed.ts) (extend) |
| T2 | `<ChapterHero>` component — rotating chapter cards with auto-advance every 7s, reduced-motion freezes to chapter 0, pause on tab blur, left/right arrow keys advance. Falls back to `<HeroFeature>` if no chapter story available. | **`apps/web/src/components/content/chapter-hero.tsx`**, **`chapter-hero.test.tsx`** |
| T3 | `<QuestStripInline>` — new inline gamification band (between hero and moods). XP ring + streak flame + level pill + 3 daily-quest pills. Authed-only; suppress for guests. | **`apps/web/src/components/feed/quest-strip-inline.tsx`**, **`quest-strip-inline.test.tsx`** |
| T4 | `<MoodSelector>` — 5 chips (Slow / High octane / Foodie / Sunrise / Art) with hand-rolled SVG icons. URL-param sync via `?mood=`. Click writes URL, server re-fetches with mood, no client-side filtering. | **`apps/web/src/components/feed/mood-selector.tsx`**, **`mood-selector.test.tsx`** |
| T5 | Restyle `<BentoMosaic>` to v3 magazine layout — 4×2 desktop, 2×2 tablet, 1×4 mobile. Feature-card spans differently per row. Reuses existing component; visual restyle only. | [apps/web/src/components/content/bento-mosaic.tsx](apps/web/src/components/content/bento-mosaic.tsx), [apps/web/src/app/globals.css](apps/web/src/app/globals.css) (`.ch-bento-grid` if needed) |
| T6 | Add `variant` prop to `<ContentCard>` (grid / horizontal / featured); restyle `<SectionRail>` as snap-scroll carousel with hover-revealed arrows at ≥1080px. | [apps/web/src/components/content/content-card.tsx](apps/web/src/components/content/content-card.tsx), [apps/web/src/components/content/section-rail.tsx](apps/web/src/components/content/section-rail.tsx) |
| T7 | `<MapStrip>` placeholder — 16:9 `ch-photo` gradient backdrop with absolute-positioned city-pin chips. Click writes `?city=` URL. Server-side renders; no JS map. | **`apps/web/src/components/content/map-strip.tsx`**, **`map-strip.test.tsx`** |
| T8 | `<CreatorSpotlight>` — featured creator card with cursor-tilt parallax (±2°), disabled on touch via `(hover: hover) and (pointer: fine)`. Reduced-motion freezes tilt. | **`apps/web/src/components/content/creator-spotlight.tsx`**, **`creator-spotlight.test.tsx`** |
| T9 | `<ContinueReadingRail>` — for authed users with content-progress, surfaces partially-read itineraries. Hidden for guests. | **`apps/web/src/components/content/continue-reading-rail.tsx`**, **`continue-reading-rail.test.tsx`** |
| T10 | Posts feed mode — when `?type=post`, page renders a single-column 640px Instagram-style feed instead of the magazine layout. Photo-carousel via existing card variants. Separate route branch in `page.tsx`. | [apps/web/src/app/page.tsx](apps/web/src/app/page.tsx) (branch), **`apps/web/src/components/feed/posts-feed-column.tsx`** |
| T11 | Save Server Action — `toggleContentSaveAction()`; optimistic flip in client, server retries, snap back + `pushToast()` on fail. | **`apps/web/src/app/actions/save.ts`** (new path), [apps/web/src/components/reader/save-button.tsx](apps/web/src/components/reader/save-button.tsx) (rewire) |
| T12 | Rewrite `apps/web/src/app/page.tsx` to compose all magazine bands in v3 order (Header · ChapterHero · QuestStrip · Moods · Bento · MapStrip · Spotlight · Continue · 11 SectionRails · Footer). Drop right-rail; use `<PageShell>` (single column) instead of `<TwoColLayout>`. ADR-0002 + SRS delta WEB-FEED-FR-023-R. | [apps/web/src/app/page.tsx](apps/web/src/app/page.tsx), **`docs/engineering/adr/0002-home-feed-single-column.md`**, **`docs/00_SRS/v1.5/srs-v1.5-r-deltas.md`** |
| T13 | 4-step review gate (edge cases → security → architecture → code quality) | [docs/epics/E5.1-home-v3/tracking.md](docs/epics/E5.1-home-v3/tracking.md) |
| T14 | Pre-commit gate: typecheck + lint + tests + Lighthouse + axe + screenshots at 5 breakpoints; commit + push to dev | — |
