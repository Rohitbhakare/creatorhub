# E5.0 — Tasks

| ID | Task | Owner | Files touched |
|----|------|-------|---------------|
| T1 | Install `framer-motion@11`; create `apps/web/src/lib/motion.ts` (easing, durations, z-index, `useGatedMotion` hook); refactor existing animations in `<ScrollReveal>` and `<PageTransition>` to consume tokens | Claude | `apps/web/package.json`, `apps/web/src/lib/motion.ts` (new), `apps/web/src/components/ui/scroll-reveal.tsx`, `apps/web/src/components/ui/page-transition.tsx` |
| T2 | Build primitives in `apps/web/src/components/ui/`: `<Btn>`, `<Pill>`, `<Tag>`, `<XPChip>`, `<StreakChip>`, `<Ring>`, `<InitialAvatar>` (port from `pack-w-chrome.jsx` + `components-primitives.jsx`); SSR-safe; Vitest snapshots per variant | Claude | `apps/web/src/components/ui/btn.tsx` (new), `pill.tsx` (new — refactor existing), `tag.tsx` (new), `xp-chip.tsx` (new), `streak-chip.tsx` (new), `ring.tsx` (new), `initial-avatar.tsx` (refactor existing if present) |
| T3 | Build layout primitives in `apps/web/src/components/ui/`: `<PageShell>` (max-width 1640, 32px gutter, 5-bp responsive), `<TwoColLayout>` (1fr + 296px right rail), `<ReaderLayout>` (200/720/280 three-col); document in WEB-DESIGN-SYSTEM.md | Claude | `apps/web/src/components/ui/page-shell.tsx` (new), `two-col-layout.tsx` (new), `reader-layout.tsx` (new), `docs/01_wireframes/v3/WEB-DESIGN-SYSTEM.md` |
| T4 | Audit + align `<WebHeader>` and `<RightRail>` against `pack-w-chrome.jsx`: search-pill, kbd-shortcut hint, theme indicator stub, RightRail v3 quests dock placeholder | Claude | `apps/web/src/components/chrome/web-header.tsx`, `apps/web/src/components/chrome/right-rail.tsx`, screens at 5 breakpoints |
| T5 | Add `<SkipLink>` to root layout; verify focus-visible 2px coral ring globally; mount `aria-live="polite"` toast region; verify `<RouteFocus>` moves focus to `<h1>` on route change | Claude | `apps/web/src/app/layout.tsx`, `apps/web/src/components/a11y/skip-link.tsx` (new), `apps/web/src/components/ui/toast-region.tsx` (new), `apps/web/src/app/globals.css` (focus ring) |
| T6 | Create `apps/web/middleware.ts` for CSRF (double-submit cookie + Origin header check); short-circuit safe verbs (HEAD/GET/OPTIONS); set CSRF cookie on response if missing | Claude | `apps/web/middleware.ts` (new), `apps/web/src/lib/csrf.ts` (new) |
| T7 | Generate `/sitemap.xml` + `/robots.txt` via Next.js metadata routes (`app/sitemap.ts`, `app/robots.ts`); list public routes; exclude `/admin/*` and authed routes | Claude | `apps/web/src/app/sitemap.ts` (new), `apps/web/src/app/robots.ts` (new) |
| T8 | Update `CLAUDE.md` (root): "Screen layout/design" reference v2 → v3. Update `apps/web/CLAUDE.md`: MVP scope expanded per SRS v1.5 (auth/feed/booking/studio in scope; `"use client"` permitted where motion or interaction requires); document new primitives | Claude | `CLAUDE.md`, `apps/web/CLAUDE.md` |
| T9 | Run 4-step review gate via slash commands: `/review-edge-cases`, `/review-security`, `/review-architecture`, then code-quality pass | Claude | `docs/epics/E5.0-web-foundation/tracking.md` |
| T10 | Update `docs/epics/TRACKING.md`: add "M2.5 — Web v3 Parity" section between M2 and V2; add E5.0 row + E5.1..E5.8 placeholder rows; document bundle deltas | Claude | `docs/epics/TRACKING.md` |
