# E5.4 — Tasks

> 11 tasks. Decisions locked: inline calendar in `<BookCta>`, SSE per-date with Postgres LISTEN/NOTIFY, canvas-confetti library, full v3 confirmation page.

| ID | Task | Files touched (new in **bold**) |
|----|------|--------------------------------|
| T1 | Audit existing booking surface — confirm Razorpay test creds in `.env`, verify `/bookings/[id]` 404s today, check the wizard's hold-timer + sendBeacon flow still works against the live API. Document gaps in [tracking.md](tracking.md#api-audit-t1). | — (read-only) |
| T2 | `<DualMonthCalendar>` — 2 side-by-side months on desktop (collapses to 1 on mobile), 7-col day grid per month, prev/next month arrows, disabled-day support (sold-out / past), keyboard nav (arrows / Home / End / PgUp / PgDn / Enter). Accepts `scheduledDates: { id, startsAt, capacity, seatsBooked }[]` and emits `onPick(dateId)`. | **`apps/web/src/components/booking/dual-month-calendar.tsx`**, **`dual-month-calendar.test.tsx`** |
| T3 | `<BookCta>` polish — when content has `scheduledDates.length > 0`, click opens inline calendar; on date pick, POST `/api/booking/start` with `scheduled_date_id` and `redirect()` to the wizard. Loading state during the POST. Same `<BookCta>` is reused on `/content/[id]` (E5.3 page already mounts it). | [apps/web/src/components/reader/book-cta.tsx](apps/web/src/components/reader/book-cta.tsx) |
| T4 | SSE seat-availability — backend `GET /api/v1/bookings/seats/stream?date_id=<id>` returns `text/event-stream` with `data: {seatsLeft, capacity}` events; subscribes to a per-date Postgres `LISTEN seat_availability_<id>` channel; backend `NOTIFY` on every booking commit / hold release. Frontend `<SeatAvailabilityStream>` opens an `EventSource` and renders a status pill (green / yellow / red / sold-out) with text label. Mounts inside the calendar's day cells + the wizard's review pane. | **`apps/api/src/routes/bookings-seat-stream.routes.ts`**, **`apps/api/src/handlers/bookings-seat-stream.ts`**, **`apps/api/src/db/migrations/035_seat_availability_notify.sql`** (trigger), **`apps/web/src/components/booking/seat-availability-stream.tsx`**, **`seat-availability-stream.test.tsx`** |
| T5 | Wizard chrome to v3 — drop the 220px stepper sidebar, replace with a horizontal stepper at top (matches W-K1 lines 13–34). Re-flow main pane to single column max-width 760 + 360px right pricing rail. | [apps/web/src/app/booking/[intentId]/booking-wizard.tsx](apps/web/src/app/booking/[intentId]/booking-wizard.tsx) |
| T6 | `/bookings/[id]/page.tsx` — confirmation page. Composition: confetti slot (T7) + booking summary card (photo + title + creator + date + travellers count + lead booker email) + pricing breakdown + Add-to-calendar dropdown (Google / Apple / Outlook URLs + .ics download from T8) + "Share with travellers" CTA (copy magic-link) + cancellation/refund policy link + "Back to bookings" link. Auth-required (redirect to `/signin?next=/bookings/{id}`). | **`apps/web/src/app/bookings/[id]/page.tsx`**, **`apps/web/src/lib/api/bookings.ts`** (extend with `fetchBookingDetail`) |
| T7 | Confetti spring — install `canvas-confetti`, mount `<ConfettiSlot>` on `/bookings/[id]?just=1`, fires on mount, gates by `useReducedMotion()` + sessionStorage one-shot key (refresh after first show doesn't re-fire). | **`apps/web/src/components/booking/confetti-slot.tsx`**, **`confetti-slot.test.tsx`** |
| T8 | `.ics` calendar export — backend `GET /api/v1/bookings/<id>.ics` returns `text/calendar` with RFC-5545-compliant body (VCALENDAR / VEVENT / DTSTART / DTEND / SUMMARY / DESCRIPTION / LOCATION / ORGANIZER / ATTENDEE for lead booker only). Special-char escaping: `\,` `\;` `\\` + CRLF. Auth-required + ownership check. | **`apps/api/src/routes/bookings-ics.routes.ts`**, **`apps/api/src/handlers/bookings-ics.ts`**, **`apps/api/src/lib/ics.ts`** + test |
| T9 | Edge cases — Razorpay `?status=cancelled` return path restores Review step (verify existing path); SSE reconnect with exp backoff; hold-expired toast already wired (verify); calendar guards against past dates + sold-out dates. | [apps/web/src/app/booking/[intentId]/booking-wizard.tsx](apps/web/src/app/booking/[intentId]/booking-wizard.tsx), [apps/web/src/components/booking/dual-month-calendar.tsx](apps/web/src/components/booking/dual-month-calendar.tsx) |
| T10 | 4-step review gate (edge cases → security → architecture → code quality) | [docs/epics/E5.4-booking-v3/tracking.md](docs/epics/E5.4-booking-v3/tracking.md) |
| T11 | Pre-commit gate: typecheck + lint + tests + 5-breakpoint screenshots × (calendar / wizard step 2 / wizard step 4 / confirmation page); commit + push to `dev` | — |
