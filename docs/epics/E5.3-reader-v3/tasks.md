# E5.3 — Tasks

> 13 tasks. Decisions locked: both modes + cookie persistence, auto drop-cap + auto pull-quote (`>`), server-side spot interleave, new `<ReaderChrome>` retiring `<StickyDayNav>`.

| ID | Task | Files touched (new in **bold**) |
|----|------|--------------------------------|
| T1 | Audit `/content/[id]` route end-to-end. Confirm 4 type renders (post / itinerary / experience / event) work, identify bits that get retired (`<StickyDayNav>`, possibly the dedicated Stops section), check whether `/api/v1/saves` supports spot-level saves or only content-level. Document gaps in [tracking.md](tracking.md#api-audit-t1). | — (read-only) |
| T2 | `<ReaderChrome>` — sticky top bar specific to multi-day reads. Composition: "← All chapters" back link, centered day-progress (e.g. "Day 1 of 4 · The Konkan Coast" + thin coral progress bar tracking scroll within current day), Aa toggle (cycles modes), Save, Share buttons. Reads `ch_reader_mode` cookie to render the right Aa state. Animates in on scroll past hero. | **`apps/web/src/components/reader/reader-chrome.tsx`**, **`reader-chrome.test.tsx`** |
| T3 | Drop-cap markdown processor — `<MarkdownBody>` extension that auto-wraps the first letter of the first paragraph (after the lead photo, if any) with `<span class="ch-dropcap">`. Skipped in Compact mode. CSS for `.ch-dropcap` lands in [globals.css](apps/web/src/app/globals.css). | [apps/web/src/components/reader/markdown-body.tsx](apps/web/src/components/reader/markdown-body.tsx), [apps/web/src/app/globals.css](apps/web/src/app/globals.css) |
| T4 | Pull-quote markdown processor — `<MarkdownBody>` overrides `<blockquote>` rendering: in Magazine mode, render as `<aside class="ch-pull-quote">` with coral 3px left-border + `--primary-tint` background + italic display font. In Compact mode, plain serif italic with no border. | [apps/web/src/components/reader/markdown-body.tsx](apps/web/src/components/reader/markdown-body.tsx), [apps/web/src/app/globals.css](apps/web/src/app/globals.css) |
| T5 | Inline-spots merge — server-side helper `mergeSpotsIntoBody(body, spots)`. Splits body markdown by paragraph; after the Nth paragraph of day D, injects a `:::spot id="<id>"` placeholder which the renderer replaces with `<InlineSpotCard>`. N = `Math.min(2, paragraphsInDay)` so the spot card surfaces early. Pure function; tested standalone. | **`apps/web/src/lib/reader/merge-spots.ts`**, **`merge-spots.test.ts`** |
| T6 | `<InlineSpotCard>` — magazine pull-out card matching v3 W-C2 lines 301–313. Two-column inside: photo on left (220h), meta on right (kicker · title · description · 2 chip tags). Accepts a heart-shaped save toggle. | **`apps/web/src/components/reader/inline-spot-card.tsx`**, **`inline-spot-card.test.tsx`** |
| T7 | Spot save microinteraction — Server Action `toggleSpotSaveAction(spotId)` (or content-level fallback if API doesn't yet support spot-level — see T1 audit). Optimistic flip with 600ms heart-spring (framer-motion); on failure rolls back via `pushToast()`. Reduced-motion gates the spring. | **`apps/web/src/app/actions/save-spot.ts`** (new), [apps/web/src/components/reader/inline-spot-card.tsx](apps/web/src/components/reader/inline-spot-card.tsx) |
| T8 | Magazine ↔ Compact mode toggle — `lib/reader-mode.ts` exports `getReaderMode()` (server, reads cookie) + `setReaderMode(mode)` (client Server Action that writes the cookie + revalidates). Page passes mode to `<ReaderChrome>`, `<MarkdownBody>`, `<InlineSpotCard>`. Magazine = drop-cap + pull-quote + serif body, generous spacing. Compact = sans body, no decorations, plain spots. | **`apps/web/src/lib/reader-mode.ts`**, **`apps/web/src/app/actions/reader-mode.ts`**, **`reader-mode.test.ts`** |
| T9 | `<PrevNextChapterFooter>` — at end of each chapter (multi-day itineraries), render two-up cards: Prev chapter ← (left, surface bg) + Next chapter → (right, ink bg). Each card shows the chapter kicker + title. Keyboard nav: `←` / `→` arrow keys focus + activate. | **`apps/web/src/components/reader/prev-next-chapter.tsx`**, **`prev-next-chapter.test.tsx`** |
| T10 | Hero polish — `<ParallaxHero>` height 70vh → 85vh, alpha-fade `[1, 1, 0.6]` → `[1, 0.85, 0.4]` for stronger title-area legibility on scroll-end. Reduced-motion still freezes. | [apps/web/src/components/reader/parallax-hero.tsx](apps/web/src/components/reader/parallax-hero.tsx) |
| T11 | Page rewrite — `apps/web/src/app/content/[id]/page.tsx`. Compose: `<WebHeader>` → `<ReaderChrome>` (multi-day) → `<ParallaxHero>` (non-post) OR blog header (post) → main 720px article column with merged inline spots → `<PrevNextChapterFooter>` (multi-day) → `<EndOfArticleRail>` → `<CommentsSection>` → `<WebFooter>`. Drop `<StickyDayNav>` import. | [apps/web/src/app/content/[id]/page.tsx](apps/web/src/app/content/[id]/page.tsx) |
| T12 | 4-step review gate (edge cases → security → architecture → code quality) | [docs/epics/E5.3-reader-v3/tracking.md](docs/epics/E5.3-reader-v3/tracking.md) |
| T13 | Pre-commit gate: typecheck + lint + tests + axe + 5-breakpoint screenshots for 4 content types × Magazine/Compact; commit + push to `dev` | — |
