# E4.1 — Tracking

**Status:** IN PROGRESS — Backend (T1–T10) + scaffold (T11–T13) + Dashboard (T14) + Users (T15) + KYC (T16) done. Next: T17 Moderation Queue.
**Progress:** 16/24 tasks done
**Branch:** `dev` (single-branch flow per CLAUDE.md)
**Last Updated:** 2026-04-22

---

## Tasks

| ID | Task | Phase | Status | Notes |
|----|------|-------|--------|-------|
| T1 | Pack J admin wireframes (10 screens) | 0 | [x] Done | Approved by founder 2026-04-22. Grouped sidebar nav added (Overview / Trust & Safety / Money / Growth / System). |
| T2 | Admin schema migration (017 + 018 seed) | 1 | [x] Done | Migrations 014–016 were already taken; used 017/018. 017 also fills the pre-existing `admin_audit_log` gap. |
| T3 | Firebase email+password verification service | 1 | [x] Done | Identity Toolkit REST wrapper + `admin-auth.service.ts` (login/change-password/provision). Generic 401 collapse for enumeration resistance. |
| T4 | Admin session middleware + auth handlers (login/logout/me/change-password) | 1 | [x] Done | `requireAdminRole` middleware w/ live is_active check; `ch_admin_session` cookie (HS256, 4h, SameSite=Strict, separate secret from JWT_SECRET). 12 integration tests pass. |
| T5 | Migrate existing admin endpoints off `x-admin-secret` | 1 | [x] Done | `dualAdminAuth(roles[])` factory accepts legacy secret OR session cookie. All 10 `admin.routes.ts` endpoints now role-scoped per plan §7. 6 new middleware tests + full suite green (869). Trust/KYC/reviews admin endpoints deferred to T23. |
| T6 | Admin user CRUD + password reset (super_admin only) | 1 | [x] Done | `admins.service.ts` + `admins.ts` handler + `/api/v1/admin/admins` router. Create/list/patch/reset-password; self-reset blocked (use /change-password); last-super-admin trigger → 409; rollback disables Firebase on DB failure. 10 integration tests. |
| T7 | Feature/unfeature toggles (ADM-FR-011) | 2 | [x] Done | 4 endpoints under `/api/v1/admin/{content,users}/:id/{feature,unfeature}`, `content_moderator`+`super_admin`. Refuses to feature content not in `published` status (409). Unfeaturing always allowed (corrective). 11 integration tests. Full suite 890/890. |
| T8 | Force-release payout endpoint | 2 | [x] Done | `POST /api/v1/admin/payouts/release` gated by `finance`+`super_admin`. Extends `releasePayout` with `{force}` opt to skip 48h timer but preserves booking-completed + no-refund-in-flight guards (409 on violation). 502 on Razorpay failure; 200+already_released when payout already `processing`. 11 integration tests. Full suite 901/901. |
| T9 | Editorial collections CRUD (ADM-FR-009) | 2 | [x] Done | `editorial.service.ts` + 7 endpoints under `/api/v1/admin/collections` gated by `content_moderator`+`super_admin`. List / create (slug uniqueness → 409) / detail (resolves `content_ids[]` preserving order) / patch (metadata + reorder) / delete / append-item / remove-item. Append refuses non-`published` content (409) and enforces 200-item ceiling. 18 integration tests. Full suite 919/919. |
| T10 | Search analytics endpoints (ADM-FR-010) | 2 | [x] Done | Migration 019 adds 3 RPCs (`search_top_queries`, `search_zero_results`, `search_click_through`) accepting `p_days` + `p_limit`. `search-analytics.service.ts` wraps them behind `/api/v1/admin/analytics/search/{top,zero-results,ctr}`, read access for all 5 admin roles. Logging hook: new `search-log.service.ts` (`logSearchQuery` / `recordSearchClick`) — fire-and-forget, 200-char truncation, swallows errors. 20 integration + unit tests. Full suite 939/939. |
| T11 | `apps/admin` Next.js scaffold | 3 | [x] Done | Next.js 15 App Router, Tailwind v4, port 3002, design tokens from v2 wireframes. Same-origin proxy (`/api/proxy/[...path]`) forwards admin session cookie so the browser talks to Hono first-party. `serverFetch()` for server components (forwards cookie from `next/headers`), `apiFetch()` for client components (via proxy). |
| T12 | Auth flow (login + change-password + middleware) | 3 | [x] Done | Edge middleware does cookie-presence gate only (no JWT secret in edge runtime). `AppShell` server component loads `/auth/me` and forces `/change-password` if `must_change_password=true`. Client forms render inline strength checks but API is source of truth. |
| T13 | Shared layout + role-scoped nav | 3 | [x] Done | `rbac.ts` groups 11 nav items across Overview / Trust & Safety / Money / Growth / System. `visibleNav(role)` filters client-side (UX only — API re-checks every endpoint). Role badge uses `--color-role-*` tokens. TopBar logout + sign-out flow. |
| T14 | Dashboard | 3 | [x] Done | New `/api/v1/admin/dashboard/summary` endpoint returns 4 counters (pending KYC / open reports / takedowns today / payouts queued) + last 10 audit entries in one trip. All 5 sub-queries run in parallel; any counter sub-query that errors surfaces as `null` so the dashboard stays partial-useful. Admin page renders stat tiles (Link-wrapped, navigate to queue) + recent-activity list with relative timestamps. 6 integration tests; API suite 945/945 green. |
| T15 | User Search + Detail | 3 | [x] Done | `/users` server-component search page (q bound to `?q=`, ilike over username/email/phone) + `/users/[userId]` detail with status badges and action buttons (Suspend / Unsuspend / Feature / Unfeature). Mutations route through a reusable `ConfirmActionModal` — Suspend requires a 10-char reason and uses the error-tone button; feature/unfeature reuse the coral CTA. No new backend; all four mutations hit existing T5/T7 endpoints via the same-origin proxy. Tabs for "recent content" and strikes deferred to a T15-follow-up. |
| T16 | KYC Queue + Detail | 3 | [x] Done | Added session-authed `/admin/kyc/:userId/{approve,reject}` + PII-safe doc redirect route. |
| T17 | Moderation Queue + Detail | 3 | [ ] Not Started | |
| T18 | Payout Run + Manual Refund | 3 | [ ] Not Started | |
| T19 | Editorial Collections (list + editor) | 3 | [ ] Not Started | Depends on T9 |
| T20 | Search Analytics + Audit Log | 3 | [ ] Not Started | Depends on T10 |
| T21 | Admin Users management screen | 3 | [ ] Not Started | Depends on T6 |
| T22 | Fly.io deployment + DNS | 4 | [ ] Not Started | |
| T23 | Remove `x-admin-secret` dual-auth | 4 | [ ] Not Started | +2 weeks after T22 |
| T24 | Docs + runbook | 4 | [ ] Not Started | |

Status markers: `[ ]` Not Started · `[~]` In Progress · `[x]` Done · `[!]` Blocked · `[-]` Deferred

---

## Review Gate

| Review | Status | Findings |
|--------|--------|----------|
| Plan approval (founder) | [ ] Not Run | Awaiting decisions on §13 Open Questions |
| Wireframe approval (founder) | [ ] Not Run | After T1 |
| Edge Cases | [ ] Not Run | |
| Security | [ ] Not Run | Extra scrutiny — admin is high-value target |
| Architecture | [ ] Not Run | |
| Code Quality | [ ] Not Run | |

---

## Pre-commit Checklist (per `.claude/instructions/precommit.md`)

- [ ] Tests written + passing (API: ~40 new tests; Admin app: ~15 smoke tests)
- [ ] Lint clean (`pnpm lint` in api + admin)
- [ ] Type check passes (`tsc --noEmit` both)
- [ ] 4-step review gate passed
- [ ] API boots (`/healthz` → 200)
- [ ] Admin app boots (`pnpm --filter admin dev` → `admin.creatorhub.in` loads)
- [ ] Tracking file updated

---

## Changelog

| Date | Change |
|------|--------|
| 2026-04-22 | Epic created. Plan drafted by Opus 4.7 per founder decision to pull V2 admin forward. Awaiting approval. |
| 2026-04-22 | Founder resolved open questions: (1) seed `rohitbhakare@gmail.com` as super_admin; auth switched from Google SSO to Firebase email+password (Google SSO deferred to E4.2) — avoids OAuth client setup cost. (2) Pure JWT, 4h TTL, no refresh. (3) Audit log kept forever. Plan updated accordingly. |
| 2026-04-22 | T1 Pack J wireframes drafted (10 screens, ~1120 lines). Wired into `CreatorHub Redesign.html` canvas (J section added). Ready for founder visual review. |
| 2026-04-22 | T1 wireframes approved; founder greenlit implementation with strong logging + exception handling. |
| 2026-04-22 | T2 migrations 017 + 018 written. 017 creates `admin_users`, `admin_audit_log`, `admin_role` enum, RLS, `guard_last_super_admin` trigger. 018 seeds `rohitbhakare@gmail.com` as super_admin. |
| 2026-04-22 | T3 Firebase Identity Toolkit REST wrapper + `admin-auth.service.ts`. All auth failures collapse to generic 401 for enumeration resistance; lockout at 20 failed logins / 24h; inactive check deferred until password proven. |
| 2026-04-22 | T4 `requireAdminRole` middleware + admin-auth handlers (login/logout/me/change-password) + routes wired under `/api/v1/admin/auth`. `ADMIN_SESSION_SECRET` isolated from `JWT_SECRET`. 12 integration tests passing. Full API suite still green (863 tests). |
| 2026-04-22 | T5 `dualAdminAuth(roles[])` middleware migrates 10 legacy admin endpoints to role-scoped session auth while keeping `x-admin-secret` as fallback for the Retool rollout window. Per-route roles per plan §7 (moderation → content_moderator+super_admin, KYC → support+super_admin, refunds → finance+super_admin, audit → any admin w/ non-super_admin scoping). Handlers prefer `c.get('adminId')` over body `admin_id`; `routeParam` helper eliminates 7 pre-existing `!` non-null assertions. Trust/KYC/reviews admin endpoints still on secret-only — migration folded into T23 cutover. +6 middleware tests, 869/869 pass. |
| 2026-04-22 | T6 `admins.service.ts` + `/api/v1/admin/admins` routes (create, list, patch, reset-password) — all super_admin-gated (no secret bypass). Temp password generated via `generateTempPassword()` and returned ONCE in the response for super_admin out-of-band delivery. Create flow: uniqueness check → Firebase provision (reuse or create) → row insert → rollback Firebase `disabled=true` on DB failure. Update translates `guard_last_super_admin` trigger rejection into 409. Reset-password blocks self-reset (handlers force admins through `/auth/change-password` instead). 10 integration tests. |
| 2026-04-22 | T7 Feature/unfeature toggles (ADM-FR-011). `admin-features.service.ts` wraps `users.featured` and `content.featured` boolean flips; guarded at route layer by `dualAdminAuth([content_moderator, super_admin])`. Featuring content requires `status='published'` (refuses draft/removed with 409) — unfeaturing is unconditional (corrective). Audit actions: `feature_user` / `unfeature_user` / `feature_content` / `unfeature_content` capture optional `reason`. 11 integration tests; full suite 890/890 green. |
| 2026-04-22 | T8 Force-release payout (ADM-FR-004). `POST /api/v1/admin/payouts/release` under `dualAdminAuth([finance, super_admin])`. Introduces `releasePayout(id, { force: true })` flag in payout.service — bypasses the 48h not_due gate but keeps booking-completed + refund-in-flight + transfer-id invariants as 409 refusals. Razorpay failures surface as 502. Idempotent re-release returns `already_released`. Audit action `force_release_payout` records `reason` + final status. 11 integration tests; full suite 901/901 green. |
| 2026-04-22 | T9 Editorial collections CRUD (ADM-FR-009). New `editorial.service.ts` + `/api/v1/admin/collections` router under `content_moderator`+`super_admin`. 7 endpoints: list / create / detail (resolves `content_ids[]` → ordered content rows) / patch (metadata + reorder) / delete / append-item / remove-item. Slug uniqueness surfaces as 409 (Postgres 23505 translation); append refuses non-`published` content and caps at 200 items; both append and remove are idempotent. Audit actions: `create_collection` / `update_collection` / `delete_collection` / `append_collection_item` / `remove_collection_item`. 18 integration tests; full suite 919/919 green. |
| 2026-04-22 | T15 User Search + Detail (ADM-FR-001..003). UI-only pass — reuses existing `/api/v1/admin/users/{search,:id,:id/suspend,:id/unsuspend,:id/feature,:id/unfeature}` endpoints via the same-origin proxy. Search page is a server component that reads `?q=` from `searchParams` and calls the Hono search endpoint with `limit=50`; results table lists users with status badges (Suspended / Creator / KYC) and a Link to the detail page. Detail renders profile + counts + action buttons, gated on current state (Suspend OR Unsuspend, never both; Feature buttons appear only for creators). All four mutations share a new `ConfirmActionModal` — Suspend requires ≥10-char reason and renders in error tone; the others use the coral CTA and take an optional reason. Modal closes on Esc, and the router refreshes after a successful action so the updated status is read straight from the API. Scope deferred to a T15-follow-up: "recent content" tab, "recent bookings" tab, strike-history view (needs a new endpoint). |
| 2026-04-22 | T14 Dashboard (ADM-FR-008). New `GET /api/v1/admin/dashboard/summary` under `requireAdminRole([all 5])` returns `{ pending_kyc, open_reports, takedowns_today, payouts_queued, recent_activity[10], generated_at }` in a single round-trip. `dashboard.service.ts` fans out five parallel Supabase queries — counts use filtered indexes (`kyc_submissions_pending_idx`, `reports_status_idx`, `payouts_status_idx`); takedowns-today counts `admin_audit_log` rows with `action='takedown_content'` since the UTC start-of-day (the audit trail is the authoritative "action taken today" source, not the content table). Each counter fails independently — a sub-query error logs + returns `null` for that tile so one blip doesn't dark the dashboard; only the recent-activity query is mandatory (throws 500 if it fails). Admin UI (`app/page.tsx` + `components/DashboardStats.tsx`) renders 4 Link-wrapped stat tiles (warn-tone in coral when a queue has work) + an activity feed with relative timestamps. 6 integration tests; full API suite 945/945 green. |
| 2026-04-22 | T11–T13 `apps/admin` scaffold. Next.js 15 App Router + Tailwind v4 on port 3002. Architecture: **same-origin proxy** (`/api/proxy/[...path]/route.ts`) relays browser → Next.js → Hono with Set-Cookie passthrough so `ch_admin_session` stays first-party in dev and behind `admin.creatorhub.in` in prod. Two fetch helpers: `serverFetch<T>()` (server components, forwards cookie from `next/headers`) + `apiFetch<T>()` (client components, via proxy). Edge middleware (`src/middleware.ts`) runs only a cookie-presence check — no JWT verification at the edge because `ADMIN_SESSION_SECRET` isn't safe to ship to the edge runtime; actual auth happens in `AppShell` server component via `/auth/me`, which also forces redirect to `/change-password` when `must_change_password=true`. RBAC nav (`lib/rbac.ts`) groups 11 items (Dashboard / Users / KYC / Moderation / Payouts / Refunds / Editorial / Search Analytics / Admins / Audit Log) and `visibleNav(role)` scopes client-side — API is still authoritative. Root scripts: `pnpm dev` now runs api+web+admin via concurrently; `pnpm dev:admin` solo. Verified: `pnpm --filter admin build` green, `pnpm typecheck` all 4 projects green, `npx eslint apps/admin/src` 0 errors; `curl :3002/` → 307 → `/login`; `curl :3002/login` → 200. T11+T12+T13 bundled as one commit — they share baseline (middleware, config, fetchers, tokens) and splitting would be churn. |
| 2026-04-22 | T10 Search analytics endpoints (ADM-FR-010). Migration 019 adds three parameterized Postgres RPCs — `search_top_queries(p_days, p_limit)`, `search_zero_results(...)`, `search_click_through(...)` — over `search_queries`. All apply the 3-char-min + phone-number regex noise filter; CTR requires ≥3 searches to hide single-query outliers. Service wraps each RPC into a typed helper; routes mounted at `/api/v1/admin/analytics/search/{top,zero-results,ctr}` with `validateQuery(analyticsWindowSchema)` (`window=7d\|30d`, `limit=1..100`) and read access for all 5 admin roles (analytics is informational). Logging hook: new `search-log.service.ts` exposes `logSearchQuery({ query, userId, sessionId, resultCount })` and `recordSearchClick({ searchId, resultId, resultType })`. Both are fire-and-forget — empty queries skip the insert, queries are trimmed + lower-cased into `normalized_query`, anything over 200 chars is truncated, and every error path swallows so a logging failure can never break a user search. Hook is exported for future search handlers to consume when they ship. 13 integration + 7 unit tests; full suite 939/939 green. |
| 2026-04-22 | T16 KYC Queue + Detail (KYC-FR-031..033). Backend: approve/reject never made it to `dualAdminAuth` in T5 (they were on the legacy `/kyc/admin/*` path). Added `handleApproveKycSession` + `handleRejectKycSession` in `admin.ts` and mounted them at `POST /api/v1/admin/kyc/:userId/{approve,reject}` under `dualAdminAuth([support, super_admin])`. Approve tolerates an empty body (new `parseOptionalJsonBody` helper) since the session cookie is sufficient — legacy callers may still send `admin_id` until T23. Reject requires a reason ≥10 chars. 8 integration tests cover 401/403 gating, session admin id forwarding, empty body for approve, and reason validation for reject. Frontend: `/kyc` queue is a server component (initial page rendered inline because `serverFetch<T>` strips the `meta` envelope needed for the cursor) that hands items + cursor to a `KycQueueList` client component with a "Load more" button; `/kyc/[userId]` detail groups identity / bank / documents / review sections with a coloured status pill, and renders doc buttons as `<a>` links to a new server route. `KycActions` reuses `ConfirmActionModal` — Approve uses the success-tone green, Reject uses the error-tone red and requires ≥10 chars. **PII-safe doc handling:** new `/kyc-doc/[userId]/[kind]` Next.js route handler (`route.ts`) server-fetches the submission via the admin session cookie, extracts `selfie_url` / `pan_doc_url` / `aadhaar_doc_url`, and issues a 302 redirect — the raw Firebase Storage URL (with its signed access token) never lands in HTML, HAR captures, or browser history auto-suggest; only resolves on click, per admin session. Admin build + typecheck + lint green; API typecheck + full suite 957/957 green. |
