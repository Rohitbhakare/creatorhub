# E5.2 — Tasks

> 13 tasks. Decisions locked: honor v3 layout (sidebar + 3-col masonry), both sidebar + drawer, vibe tags from API, 3 working sort tabs + 1 "Coming soon" Top creators tab.

| ID | Task | Files touched (new in **bold**) |
|----|------|--------------------------------|
| T1 | Audit `/discover` API surface — confirm `?sort=trending\|recent\|near` works on `/api/v1/discover/category`, verify type-counts query returns `{ post: N, itinerary: N, experience: N, event: N }` for the sidebar Type pills, capture which `themes`/popular-tags endpoint feeds the Vibe rail. Document gaps in [tracking.md](tracking.md#api-audit-t1). | — (read-only) |
| T2 | `<DiscoverHeader>` — editorial H1 ("What's calling you?"), one-line lede, live stats row ("12 487 stories · 312 creators · 24 cities") rendered server-side with real counts. | **`apps/web/src/components/discover/discover-header.tsx`**, **`discover-header.test.tsx`** |
| T3 | `<DiscoverSidebar>` — 240px persistent column on `≥1080px`, collapses to top-pill row on mobile. Sections: Type pills (with counts) · Vibe tags (top 8 from API) · Distance slider (URL-synced). Bottom link "+ More filters" opens drawer (T7). All filters write to URL params; SSR re-renders from URL. | **`apps/web/src/components/discover/discover-sidebar.tsx`**, **`discover-sidebar.test.tsx`** |
| T4 | `<DiscoverGrid>` — 3-col CSS grid with `gridAutoFlow: 'dense'` and 1×/2× spans. Uses existing `<ContentCard variant="grid">` (E5.1) inside grid items. "Load 24 more" CTA at the bottom server-paginates via `?page=`. Reuses existing API. | **`apps/web/src/components/discover/discover-grid.tsx`**, **`discover-grid.test.tsx`** |
| T5 | `<DiscoverSortTabs>` — 4 tabs (Trending / Recent / Near me / **Top creators** with "Coming soon" badge). Wired to `?sort=` URL param; "Top creators" disabled until API support lands (filed as ENH-001). | **`apps/web/src/components/discover/discover-sort-tabs.tsx`**, **`discover-sort-tabs.test.tsx`** |
| T6 | `<ActiveFilterChips>` — chip row above grid showing the resolved active filters from URL state (Type, Vibe, Distance, plus drawer-only filters). Click × to remove (writes URL without that param); "Clear all" link on the right. (FR-036) | **`apps/web/src/components/discover/active-filter-chips.tsx`**, **`active-filter-chips.test.tsx`** |
| T7 | "+ More filters" drawer — restyle existing `<FilterSheet>` to render as a **420px right-side drawer on desktop**, bottom-sheet on mobile. Mounts all 13 filter groups (FR-034). Focus-trapped, `Esc` closes, `aria-modal`. Apply button writes URL params, drawer state syncs from URL on mount. | [apps/web/src/components/discover/filter-sheet.tsx](apps/web/src/components/discover/filter-sheet.tsx) (restyle), **`filter-drawer.test.tsx`** (new) |
| T8 | Restyle `<HandpickedCollectionsRail>`, `<TopCreatorsRail>`, `<UpcomingExperiencesRail>` to sit **below** the masonry grid as secondary editorial bands. Use `<RailScroller>` from E5.1; reuse `<ContentCard>` variants. | [apps/web/src/components/discover/handpicked-collections-rail.tsx](apps/web/src/components/discover/handpicked-collections-rail.tsx), [apps/web/src/components/discover/top-creators-rail.tsx](apps/web/src/components/discover/top-creators-rail.tsx), [apps/web/src/components/discover/upcoming-experiences-rail.tsx](apps/web/src/components/discover/upcoming-experiences-rail.tsx) |
| T9 | Restyle `/discover/results` (deep-link search results) to share the same chrome — sidebar + grid + sort tabs. The query input lives in the header; URL params unchanged. | [apps/web/src/app/discover/results/page.tsx](apps/web/src/app/discover/results/page.tsx) |
| T10 | Page rewrite — `apps/web/src/app/discover/page.tsx`. Compose: `<WebHeader>` · `<DiscoverHeader>` · two-column layout (`<DiscoverSidebar>` + `<main>` containing `<DiscoverSortTabs>` · `<ActiveFilterChips>` · `<DiscoverGrid>`) · 3 secondary rails · `<WebFooter>`. Drop the rails-heavy current layout. | [apps/web/src/app/discover/page.tsx](apps/web/src/app/discover/page.tsx) |
| T11 | Cmd+K polish — verify the existing `<CommandPalette>` chrome matches the v3 wireframe (`pack-w-discover.jsx` lines 151–242). Adjustments are visual-only; keep the existing keybinding + lazy-load split. | [apps/web/src/components/discover/command-palette.tsx](apps/web/src/components/discover/command-palette.tsx) |
| T12 | 4-step review gate (edge cases → security → architecture → code quality) | [docs/epics/E5.2-discover-v3/tracking.md](docs/epics/E5.2-discover-v3/tracking.md) |
| T13 | Pre-commit gate: typecheck + lint + tests + axe + 5-breakpoint screenshots; commit + push to `dev` | — |
